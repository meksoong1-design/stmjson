# Environments
.env
.venv
env/
venv/
ENV/
env.bak/
venv.bak/

# Streamlit secrets (ถ้ามีการตั้งรหัสผ่านในอนาคต)
.streamlit/secrets.toml

# Python cache files
__pycache__/
*.py[cod]
*$py.class
*.so

# Jupyter Notebook (ถ้ามีการทดสอบโค้ด)
.ipynb_checkpoints

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# -------------------------------------------------------------
# 🔒 SECURE DATA (สำคัญมาก ห้ามเอาขึ้น Git เด็ดขาด!)
# -------------------------------------------------------------
# Data files & User Uploads
*.json
*.xlsx
*.xls
*.csv
*.pdf

# ยกเว้นโฟลเดอร์สำหรับไฟล์ตัวอย่าง (ถ้ามีให้เพิ่ม ! ข้างหน้า)
# !sample_data/*.json
